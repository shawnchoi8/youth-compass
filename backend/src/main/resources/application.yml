spring:
  application:
    name: youth-compass-backend

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/youth_compass}
    username: ${SPRING_DATASOURCE_USERNAME:admin}
    password: ${SPRING_DATASOURCE_PASSWORD:adminpassword}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

server:
  port: 8080
  servlet:
    context-path: /api

# AI Service URL
ai:
  service:
    url: ${AI_SERVICE_URL:http://localhost:8000}

# Logging
logging:
  level:
    com.youthcompass: DEBUG
    org.springframework: INFO
    org.hibernate: INFO